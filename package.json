{
  "scripts": {
    "start": "php artisan serve",
    "clean-up": "php artisan cache:clear && php artisan route:clear && php artisan config:clear && php artisan view:clear",
    "update": "php composer.phar update"
  },
  "private": true,
  "devDependencies": {
    "gulp": "^4.0.2"
  },
  "dependencies": {
    "bootstrap-sass": "^3.0.0",
    "laravel-elixir": "^5.0.0"
  }
}
